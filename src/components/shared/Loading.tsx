import styled from "styled-components";

const Wrapper = styled.div`
  position: inherit;
`;

export default function Loading() {
  return (
    <Wrapper>
      <div>Loading</div>
    </Wrapper>
  );
}
